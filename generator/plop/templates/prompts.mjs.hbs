import { {{name}}Tempates } from './templates.mjs';

export function {{name}}Prompts() {
  return [
    {
      type: 'list',
      name: 'template',
      message: 'Choose a template',
      choices: Object.values({{name}}Tempates),
    },
    {
      when(context) {
        return context.template.includes({{name}}Tempates.endpoint);
      },
      type: 'input',
      name: 'name',
      message: 'Choose a name',
    },
  ];
}
