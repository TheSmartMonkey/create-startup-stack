/**
 * @group unit
 */
import { describe, expect, test } from '@jest/globals';
import { initUnitTests } from '@tests/helper';

describe('{{titlecase name}} unit', () => {
  beforeAll(() => {
    initUnitTests();
  });

  test('should create', async () => {
    // Given
    // When
    // Then
    expect(true).toEqual(true);
  });
});
